# Memory Index
- [Handoff: invoice/chat/record-keeping](handoff-invoice-chat-recordkeeping.md) — ⚠️ HANDOFF (2026-06-16→19): invoice-timeout false-fail fix, chat delete-crash fix (cp-040), chat "Delete & rewrite" (cp-041), Source-in-edit (cp-039). ⚠️ UNCONFIRMED prod steps gate it: `migrate --force` + `npm run build` + `config:clear`. Optional in-app icon legend offered, not built. READ FIRST when continuing this work in the new workspace.
- [DuoSync shared Claude memory](duosync-shared-memory.md) — one pool shared by Marketing + Dashboard + the suite workspace, synced via the coord repo
- [DuoSync coord silent divergence](duosync-coord-silent-divergence.md) — hooks swallow git errors; local coord clone can silently fall behind origin
- [Chat media ADR-0007](chat-media-adr0007.md) — accountant↔client chat photos/voice/files; both dashboard halves built+deployed; the inbound EMPTY-BUBBLE fix (file & message sync in different polls → bidirectional link + read-time self-heal in ChatService::threadFor + inbound-only guards; commits 96a8c56→21f91bb). Prod owes one-time optimize:clear + queue:restart; only the joint smoke test remains. READ before touching chat media / mobile message+attachment projectors.
- [Mobile poll queue fix](mobile-poll-queue-fix.md) — ⚠️ mobile transactions stopped syncing into Record Keeping: PollMobileChangesJob was on the `mobile-sync` queue but the worker (+manual `queue:work`) only drains `default` → poll never ran (a client's income never appeared). Fixed on dev (config queue→default + projector self-heal); NOT yet committed/deployed — Amin to test `mobile:poll --sync --force` first. Durable gotcha: jobs on a non-`default` queue silently never run in prod. READ before touching mobile sync / queues.
- [Colleague 4-branch integration (Jun 2026)](colleague-branch-integration-2026-06.md) — integrated accounting-provider-qbo + books-smart-import + phase-8b-public-community + phase7-text-match-refactor into main one-at-a-time; prod deploy hit chk_activity_logs_entity_type violation (missing ClientMessage) → fixed in da12194 (superset all 3 constraint migrations). ⚠️ CONFIRM prod deploy actually finished. READ before deploying activity_logs constraint migrations.
- [Attachment file-size fix](attachment-file-size-fix.md) — file size now correct everywhere (2026-06-27, commit f6c5cbc): mobile attachments backfilled from S3 HEAD (feed sends file_size=0) + chat bubbles render size. ⚠️ temp /testfilesize + /testfilecheck routes LIVE on prod — remove after verify. Prod owes build + mobile:project-attachments + queue:restart. DQ-0015 asks mobile to populate file_size at source. READ before touching attachment size / mobile projector / chat media.
- [Clients redesign plan](clients-redesign-plan.md) — agreed IA (2026-06-25): client detail → tabbed Client Workspace (kill giant modal); Companies(vendors) folded in as a per-client tab w/ firm-wide toggle (leaves sidebar); refined directory cards; 5 onboarding buttons → 1 "Add client" + menu. Phased build, not started. READ before building clients.
- [Wait for user test before deploy](wait-for-user-test-before-deploy.md) — ⚠️ from 2026-06-25: make change + build, then STOP and wait for the user to test before checkpoint/deploy. Don't auto-push.
- [Global AI Assistant agent](global-ai-assistant-agent.md) — corner AI widget → full confirm-gated agent (answer+tickets→navigate→create tx→create/find client) on dashboard Gemini; Phase 1 (Gemini swap, off Anthropic) done in-repo. Includes local Windows PHP→Gemini stall gotcha. READ before touching AiAssistantService / AiAssistantChat.
- [Record Keeping AI Voice-to-Edit](record-keeping-ai-voice-edit.md) — Record Keeping redesign; voice→AI field-diff→human-approve reusing Gemini; 7 locked decisions + 4-phase plan; Phase 1 building. READ before touching the Record Keeping detail panel or voice features.
- [Client workspace architecture](client-workspace-architecture.md) — client-as-primary workspace + accountant-connection lifecycle + billing; 3-phase build. Phase 1 DONE (pivoted at checkpoint-027: a client = single-client accountant SESSION using the REAL accountant pages pinned to one client; client can submit New Transaction + inline ledger evidence; own-company Settings). Phase 2 (self-serve personal workspace) = NEXT. READ before touching the client portal.
- [DuoSync setup](duosync-setup.md) — three-way Claude coord active (owner Amin, coord at E:/Projects/VA-Dashboard-coord, Windows python3→py -3 shim). Now on BOTH dashboard + marketing repos (2026-06-26).
- [Marketing site suite](marketing-site-suite.md) — two-project suite (voiceaccountant.com marketing + Laravel dashboard); SEO/LLM is marketing-only; platform decision (Statamic/WordPress/Astro) PENDING; NEVER merge/embed marketing into Laravel. READ before marketing/SEO/hosting work.
- [Local dev run (Windows)](local-dev-run-windows.md) — `composer dev` breaks on Windows 3 ways (pail/pcntl kills stack, Vite [::1] white screen → fixed in vite.config.js, single-threaded serve → "Failed to fetch"). Use the worked launch command. READ before "run localhost".
- [Google OAuth setup](google-oauth-setup.md) — Drive import + Sign-in-with-Google share one NEW OAuth client in project `voice-accountant` (mobile creds untouched); 3 GOOGLE_* in .env only; localhost≠127.0.0.1 gotcha (both needed in origins/redirects/Picker-referrers). READ before touching Google auth/Drive.
- [Document AI pipeline](document-ai-pipeline.md) — dashboard's OWN Gemini bulk-extraction (Batch Upload AI toggle → draft pending source=ai transactions), Laravel-native, separate from mobile n8n. Phase 1 DONE + verified e2e. Phase-2: real Economy batch-API + scanner/Drive wiring. READ before touching Document Hub upload or the Gemini services.
- [Transaction source origin rules](transaction-source-origin-rules.md) — spec (NOT built yet): Source badge should label by ORIGIN — 📱 mobile (Client/AI), 👤 client-workspace (Client), 👤 accountant-dashboard (Accountant). Distinguish via transactions.mobile_id. Current SOURCE_META only keys off source enum.
- [Auto-commit leaks secrets](autocommit-leaks-secrets.md) — ⚠️ an auto-commit process pushes this repo's working tree to GitHub WITHOUT review (already leaked DB passwords + a live AWS key via temp/). NEVER write secrets to any path inside the repo. READ before saving any credentials or pushing.
- [Checkpoint rule](checkpoint-rule.md) — "add a checkpoint" = commit (checkpoint-NNN + title) + annotated tag + push branch & tag. Latest: checkpoint-054 (ADR-0008 dashboard half — client invites a not-yet-registered accountant: D1 = RegisterFirmInMobileDirectoryJob registers the new firm in mobile's directory at sign-up (carries profile.email) so mobile's email-match AdoptStub adopts the invite stub and the inherited 'invited' row surfaces in /inbox; D2 = existing ADR-0004 Inbox accept/decline, no code change; DEPLOYED @ bec0f92). Prior: checkpoint-032 (app↔web link flow — DASHBOARD HALF OF ADR-0006 COMPLETE: #1 directory-register personal firm (is_personal) + #4 "Link your app account" 6-digit Tier-2 code on Billing → resolveLinkCode → set clients.mobile_user_id → poll; code landed in 5edbe23 bundled w/ sibling's temp /testfilecheck diagnostic; prod owes migrate + build). checkpoint-031 (SubscriptionProjector — ADR-0006 #2: mobile's `user_subscriptions` feed → personal firm `subscription_expires_at`, unlocking the workspace; non-personal firms no-op; + mobile:project-subscriptions cmd). checkpoint-030 (real email verification for self-serve personal signups — ADR-0006 dashboard pre-req #3 gating Tier-1 app↔web linking: User implements MustVerifyEmail + personal signups no longer auto-verified + backfill migration grandfathers existing users; prod owes `php artisan migrate` + `npm run build`). checkpoint-029 (post-trial guard — a lapsed PERSONAL workspace is sent to /client/billing with a "subscribe" prompt instead of "contact your accountant"; ADR-0006 client-IAP-subscriptions APPROVED by both admins, READY TO BUILD — see [[union-plan-coordination]]). checkpoint-028 (Phase 2 foundation — self-serve personal workspace: register as "Individual" → hidden personal firm (is_personal, 14-day trial) + own Client + type=client user → client experience; PersonalWorkspaceService + RegisteredUserController branch; paid checkout deferred; prod owes `php artisan migrate` (transaction_notes + is_personal) + `npm run build`). checkpoint-027 (client workspace PIVOT — client = single-client accountant session on the REAL accountant pages pinned to one client). checkpoint-026 (client workspace Phase 1 — client portal at same /login, read-only ledger + 3 writes). checkpoint-025 (installed league/flysystem-aws-s3-v3 — S3 driver was NEVER a declared dep; prod owes `composer install`. S3 attachment viewing FIXED (commit f300c04). The chain: (1) IAM read perms — Amin fixed in AWS console, confirmed (lists 64 folders + 588 root files + GETs). (2) Real root cause = a KEY-PATH MISMATCH, not missing binaries: mobile `file_url` is `…/{user_id}/{filename}` but objects are stored in the bucket ROOT under just `{filename}` (temp route `/temp/s3-attachment-test?deep=1` proved 25/25: `{user_id}/<file>`→404, bare `<file>`→206). FIX: `Attachment::resolveStorageKey()` tries stored path then falls back to basename; `signedUrl()` + `DocumentsController::serve` (now disk-aware) + `ClientPortalController::serveDocument` all use it — NO re-projection needed. SECOND bug found via the accountant ReceiptViewer returning `[]` from /recordkeeping/{id}/attachments: projector stored `attachable_type` = FQCN (`App\Models\Transaction`) but the whole dashboard + morphMap query the ALIAS `'Transaction'` → query missed. Fixed (commit 6a74205): projector stores 'Transaction'/'Client' + update branch normalizes legacy FQCN rows on re-sync. **Prod must re-run `php artisan mobile:project-attachments` to normalize the 25 existing rows.** Accountant receipt viewing works after that re-run (backend-only). Client-portal pages (ClientPortal/*) are NEW (checkpoints 026-029) so prod still owes `npm run build` + `php artisan migrate --force` to render them. Temp diagnostic route `/temp/s3-attachment-test` still live — REMOVE after final confirmation. composer install + MOBILE_S3_* env already done on prod.). checkpoint-024 (VIEW mobile attachments — read-only mobile_s3 disk + presigned login-gated /attachments/{id}/preview route; prod owes MOBILE_S3_* in .env + config:clear + re-run mobile:project-attachments). checkpoint-023 = attachment projection (records file_url + tx link); checkpoint-022 = transaction projection; checkpoint-020 = inbound chat (ADR-0004). checkpoint-019 = session lifetime 30 days (Remember-me logout fix; prod .env needs SESSION_LIFETIME=43200 + config:clear).
- [Dashboard build status](dashboard-build-status.md) — feature arc (inbox/invoicing/chat + Messages Studio redesign on the mobile bridge), checkpoints 003–012, gotchas (prod M2M key, MOBILE_FAKE_WRITES, deploy/lockfile), open items. READ AT SESSION START.
- [Mobile attachment binaries missing](mobile-attachment-binaries-missing.md) — mobile PDFs/recordings mirror as rows but the S3 objects don't exist (404 NoSuchKey, right bucket); confirmed mobile-side, dashboard correct; raised as DQ-0014. Use `mobile:attachment-doctor` to verify before touching dashboard code.
- [Feature test handoff](feature-test-handoff.md) — every feature: give Amin a manual test guide + where to inject fake data on BOTH mobile and dashboard.
- [Document each change](document-each-change.md) — every important change/feature gets a docs/ markdown (what changed + how it works), done with the same checkpoint.
- [Deploy process](deploy-process.md) — deploy webhook only does git pull; migrations + npm build must be run manually on prod (Amin via SSH).
- [Union-plan coordination](union-plan-coordination.md) — I'm "Dashboard Claude" coordinating with Mobile Claude via E:/Projects/union-plan-coord; one-way mobile→dashboard poll-sync; ADRs 0001–0003 approved, auth round-trip GREEN, STATUS = READY TO BUILD; DQ-0010 (inbound chat) SHARPENED into a build-ready contract (ball on Mobile Claude → ADR-0004).
- [International direction](international-direction.md) — product going international; i18n must be RTL-safe + ICU + TMS-friendly from day one; white-label & walkthrough discussed but advisory-only.
- [Upload ghost-row bug](upload-ghost-row-bug.md) — dashboard `s3` disk is read-only (MOBILE_S3 fallback when AWS_BUCKET blank) + `throw=>false` → silent put() failure made all 6 ingest paths create attachment rows with no bytes ("preview not available"). Fixed via guarded `storeUpload()` (check put() + fall back to local). READ before touching upload/storage code.
- [Handoff: mobile attachment visibility](handoff-mobile-attachment-visibility.md) — ⚠️ HANDOFF (chat ~2026-06-13, for the new workspace): made mobile receipts/voice notes viewable for accountant + client via 3 fixes — flysystem-aws-s3-v3 dep (cp-025), S3 key resolver basename-fallback (f300c04), attachable_type FQCN→morph-alias + re-sync normalize (6a74205). Full detail in repo `docs/mobile-attachment-visibility-handoff.md`. Temp route `/temp/s3-attachment-test` owed removal. VERIFY current state before re-running old prod steps. READ when continuing attachment-visibility work.
- [Client workspace: firm tools + Inbox overhaul](client-workspace-firm-tools-and-inbox.md) — 2026-06-27: client portal Messages→"Inbox" (attachments render + client can send files & record voice; "Invitation"/notifications folded in as a pinned rail item) + Books/Report/Accounting/Integration added to the client workspace, each scoped per-client (new `role.min.client` middleware + ResolveTenant firewall + QB index scoping). Includes the reusable "expose a firm tool to clients safely" pattern. On origin/dev, NOT deployed (awaiting Amin test). READ before touching the client portal or giving clients a firm tool.
- [Deployment guide](deployment-guide.md) — authoritative deploy/infra doc at docs/DEPLOYMENT.md (committed, travels via git); Supervisor→queue-only, cron→schedule:run-only; Docker→Railway (3 services, schedule:work not Railway-Cron, Pre-Deploy/railway-ssh for Artisan). MUST keep updated. READ before deploy/Docker/Railway work.
- [S3 write + Railway migration](s3-write-railway-migration.md) — ⚠️ IN-PROGRESS: prod S3 key is read-only → uploads silently fall to local disk (LOST on Railway redeploy); dashboard files MUST go to S3 under firms/* before migrating; IAM PutObject fix + TEMP /temp/s3-write-test verify route to REMOVE. READ before Railway migration / uploads / storage.
- [CLAUDE.md must stay tracked + .gitignore gotcha](claude-md-gitignore-tracking.md) — 2026-06-27: removed the dead `claude.md` .gitignore rule (since initial commit) that greyed out the tracked CLAUDE.md on Windows (commits e9dfb5a→64b1967 on dev). `.gitignore` never affects already-tracked files; VS Code dimming is just visual → Reload Window. CLAUDE-kamyar.md was a byte-identical duplicate, removed. Don't re-ignore CLAUDE.md. READ before touching .gitignore / CLAUDE.md / "merge two CLAUDE.md" tasks.
- [Email Integration (forwarding)](email-integration-forwarding.md) — Dashboard inbound-email forwarding: forward a receipt to a unique per-client address → AI draft `source=email` + accountant-gated match proposal. Receiving switched **Resend→AWS SES** (2026-06-30, SES→S3+SNS→/webhooks/ses/inbound→SQS→parse MIME); sending stays Resend. Resend version @ checkpoint-115; SES switch coded on dev, NOT committed. Amin owes AWS SES/S3/SNS/SQS infra. `email:simulate-inbound` tests it without AWS. READ before touching inbound email / SES / SNS / the match banner.
- [pr staging box](pr-staging-box.md) — pr.voiceaccountant.com = dev-branch staging on the prod server; root-.htaccess→public bridge; deploy = composer install+npm build+storage tree; now has its OWN copy DB `va_dashboard_pr` (no longer shares prod — safe to migrate); creating a pg DB here needs the postgres superuser (hlrihub has no sudo/docker, va_user lacks CREATEDB)
- [Email integration forwarding](email-integration-forwarding.md) — Resend inbound → Gemini extractor → draft source=email tx; checkpoint-115 on dev, prod NOT deployed (Amin doing infra 2026-06-30). READ before touching inbound email / EmailInbox / ProcessInboundEmail.
- [Bigcapital deployment](bigcapital-deployment.md) — live on Railway `vadash-bigcapital-rt`; API at server-production-406c.up.railway.app; JWT_SECRET weak (fix); migration path is build/ not dist/
- [DuoSync code sync behavior](duosync-code-sync-behavior.md) — hooks now sync code repo too: merge main→dev on start, push dev to origin on end. Branch-guarded (dev only). VA-Dashboard hooks need same update.
- [Payroll & HR architecture master plan](payroll-hr-architecture.md) — NOT YET BUILT. Stack: Bigcapital + Frappe HR + custom payroll engine. Canada (CRA formulas) + US (IRS/state tables). ADP deferred until scale. READ BEFORE starting any payroll work.
- [Prod white screen = Vite hot file](prod-vite-hot-white-screen.md) — my.voiceaccountant.com blank because a leftover `public/hot` on the PROD server put `@vite` in dev mode (emits 127.0.0.1:5173 tags) → JS never loads. Built assets were fine. Fix: `rm -f public/hot` + `optimize:clear` on prod, hard-refresh. Never `npm run dev` on prod. READ before touching deploy/Vite/white-screen.
- [Dashboard nav performance](dashboard-nav-perf.md) — slow page-switching = no Inertia prefetch + no persistent layout (all 50 Pages inline-wrap AuthenticatedLayout → whole shell remounts each nav). Fix #1 (prefetch on Sidebar links) DONE local/uncommitted; Fix #2 (persistent-layout refactor) PLANNED. READ before touching dashboard navigation speed / AuthenticatedLayout.
- [Dashboard nav performance](dashboard-nav-perf.md) — slow page-switching = no Inertia prefetch + no persistent layout (all 50 Pages inline-wrapped AuthenticatedLayout → whole shell remounted each nav). BOTH fixed + pushed to main in commit f27e462 (2026-07-01); prod owes `npm run build`. READ before touching dashboard navigation speed / AuthenticatedLayout.
- [Batch Upload + AI tester fixes](batch-upload-ai-tester-fixes.md) — ⚠️ 2026-07-02, BUILT NOT COMMITTED on dashboard main: chunked uploads (25-file cap gone UI-side), per-file ai_extract_status + retry + ledger auto-refresh, ClientAiContext direction fix (± chip), Kind/Counterparty columns, thinkingBudget=0 + single-call extraction. Awaiting Amin's test → checkpoint; prod owes migrate+build+queue:restart+workers+paid Gemini key. READ before touching BatchUploadModal / document-AI / TransactionsTable.
- [AI usage monitoring](ai-usage-monitoring.md) — per-user/per-feature/per-step AI token+cost ledger (3 tables + admin API + 2 additive sections in the untracked `index 3.html`); built & verified 2026-07-09 (39+32 assertions), NOT deployed. Encodes the token-normalisation traps (Gemini prompt INCLUDES cached; thinking bills at the OUTPUT rate) and the scoped-not-singleton context rule. READ before touching AI cost, GeminiClient recording, ai_prices, or the admin analytics API.
- [AI cost optimization](ai-cost-optimization.md) — 2026-07-07 Gemini spend cuts via DocumentAiExtractor: local image downscale (GD, ~87% fewer bytes) + digital-PDF text layer (pdftotext) + prompt caching reorder + usageMetadata logging, all config-gated & fail-safe; OCR hybrid (Tesseract) BUILT but OFF by default w/ strict confidence gate — never enable in prod untested; Economy/Batch 50%-off already wired (use it). NOT deployed. READ before touching Gemini cost / DocumentAiExtractor.
- [Gemini model policy](gemini-model-policy.md) — ⚠️ MANDATORY (Amin, 2026-07-06): dashboard uses gemini-flash-lite-latest for ALL Gemini calls (extract + file); gemini-2.5-flash retired; never hard-code a model, read services.gemini config; prod .env may still pin the old model. READ before touching any Gemini service.
- [Multi-currency FX](multi-currency-fx.md) — ✅ DEPLOYED checkpoints 119-122 (prod migrated + fx:backfill run + live-verified 2026-07-04→06): model-A normalisation (amount/currency = client base, original preserved), observer→ConvertTransactionCurrencyJob on ALL intake paths, invoice-date rates via BoC+ECB free chain + fx_rates permanent cache, 32 auto-convertibles / 13 manual-only (IRR-style amber row), fx:backfill, self-serve portal Books-currency card + FX history catch-up button (portal + accountant, shared FxHistoryConverter) + portal soft-delete (cp-122 9949250). READ before touching amounts/currency/reports/projector.
- [Batch Upload + AI tester fixes](batch-upload-ai-tester-fixes.md) — ✅ DEPLOYED (checkpoints 117-118, 2026-07-04): chunked uploads (25-file cap gone UI-side), per-file ai_extract_status + retry + ledger auto-refresh, ClientAiContext direction fix (± chip), Kind/Counterparty columns, thinkingBudget=0 + single-call extraction; cp-120 added worker-timeout fix (job $timeout > internal HTTP budget rule), cp-121 the Gemini currency de-bias. Amin still owes prod php.ini bumps + optional Supervisor AI pool (--queue=ai ×6). READ before touching BatchUploadModal / document-AI / TransactionsTable.
- [Checkpoint rule](checkpoint-rule.md) — "add a checkpoint" = commit (checkpoint-NNN + title) + annotated tag + push branch & tag. Per-checkpoint history lives in git tags + the topic file, not here. Latest noted: checkpoint-054 (ADR-0008 dashboard half — client invites a not-yet-registered accountant; DEPLOYED @ bec0f92). READ the topic file for the full checkpoint log (019→054) when you need a specific one.
- [activity_logs entity_type trap](activity-log-entity-type-trap.md) — ⚠️ an entity_type/action not in the Postgres CHECK allow-list rolls back the WHOLE transaction and surfaces as an unrelated field error; sqlite dev skips CHECK so it only bites prod. Bit us twice (ClientMessage @ migrate; `ReferralAccepted` broke ALL /refer client onboarding, fixed cp-124). READ before adding a new entity_type/action or debugging a prod-only "always fails" write.
- [Document Hub folders redesign](document-hub-folders.md) — real folders + AI auto-filing + Trash/30d-purge + Explorer UI on /documents (dashboard + client workspace); ✅ DEPLOYED checkpoint-139 (2026-07-13). Unsorted = folder_id NULL; user filing sticky; client moves only own-side files. ⚠️ Resend email work deliberately left uncommitted (partial stage) — needs its own checkpoint. READ before touching /documents, DocumentFolder, or doc classification.
- [activity_logs entity_type trap](activity-log-entity-type-trap.md) — ⚠️ entity_type/action outside the Postgres CHECK allow-list rolls back the WHOLE transaction and surfaces as an unrelated error; sqlite dev skips CHECK. Bit us twice. READ before adding entity_type/action or debugging a prod-only "always fails" write.
- [AI usage monitoring](ai-usage-monitoring.md) — per-user/feature/step AI token+cost ledger + admin API + Gemini call logs (gzip payloads, 30d prune); built & verified 2026-07-09/10, NOT deployed. Token-normalisation traps + scoped-not-singleton rule. READ before touching AI cost, GeminiClient recording, ai_prices, call logging, or admin analytics.
- [AI cost optimization](ai-cost-optimization.md) — 2026-07-07 Gemini spend cuts (image downscale, PDF text layer, prompt-cache reorder, usage logging; Tesseract OCR built but OFF). NOT deployed. READ before touching Gemini cost / DocumentAiExtractor.
- [Gemini model policy](gemini-model-policy.md) — ⚠️ MANDATORY: gemini-flash-lite-latest for ALL calls; never hard-code a model, read services.gemini config. READ before touching any Gemini service.
- [Gemini repetition-loop cost bleed](gemini-repetition-loop-cost.md) — ⚠️ extraction loops → 85K output tokens/call; fixed 2026-07-11 w/ maxOutputTokens + dedupeRows; Gemini rejects schema maxItems. NOT deployed. READ before touching extractor generationConfig / extraction cost.
- [Multi-currency FX](multi-currency-fx.md) — ✅ DEPLOYED cp119-122: base-currency normalisation, ConvertTransactionCurrencyJob on all intake, BoC+ECB rates + fx_rates cache, fx:backfill, portal FX cards. READ before touching amounts/currency/reports/projector.
- [Batch Upload + AI tester fixes](batch-upload-ai-tester-fixes.md) — ✅ DEPLOYED cp117-118 (+120 worker-timeout rule, +121 currency de-bias): chunked uploads, per-file ai_extract_status+retry, single-call extraction. Amin owes prod php.ini bumps + optional AI worker pool. READ before touching BatchUploadModal / document-AI / TransactionsTable.
- [Prod white screen = Vite hot file](prod-vite-hot-white-screen.md) — leftover `public/hot` puts @vite in dev mode → blank site. Fix: rm public/hot + optimize:clear. Never npm run dev on prod.
- [Dashboard nav performance](dashboard-nav-perf.md) — slow nav = no Inertia prefetch + no persistent layout; BOTH fixed in f27e462 (2026-07-01); prod owes npm run build. READ before touching nav speed / AuthenticatedLayout.
- [Email Integration (forwarding)](email-integration-forwarding.md) — inbound email → AI draft source=email + match proposal. Pluggable EMAIL_INBOUND_DRIVER: resend ACTIVE, ses built but PARKED; serves accountant + client workspace; `email:simulate-inbound` tests locally. Dual-driver on dev NOT committed. READ before touching inbound email / driver / match banner / Settings forwarding card.
- [DuoSync shared Claude memory](duosync-shared-memory.md) — one memory pool shared by Marketing + Dashboard + suite, synced via coord repo.
- [DuoSync coord silent divergence](duosync-coord-silent-divergence.md) — hooks swallow git errors; local coord clone can silently fall behind origin.
- [DuoSync setup](duosync-setup.md) — three-way coord active (coord at E:/Projects/VA-Dashboard-coord; Windows python3→py -3 shim); on dashboard + marketing repos.
- [DuoSync code sync behavior](duosync-code-sync-behavior.md) — hooks also sync code: merge main→dev on start, push dev on end (dev-branch guarded).
- [Handoff: invoice/chat/record-keeping](handoff-invoice-chat-recordkeeping.md) — ⚠️ cp039-041 fixes gated by UNCONFIRMED prod steps (migrate+build+config:clear). READ when continuing that work.
- [Chat media ADR-0007](chat-media-adr0007.md) — chat photos/voice/files built+deployed; inbound empty-bubble fix (bidirectional link + read-time self-heal). Prod owes optimize:clear + queue:restart; joint smoke test remains. READ before touching chat media / mobile projectors.
- [Mobile poll queue fix](mobile-poll-queue-fix.md) — ⚠️ PollMobileChangesJob sat on `mobile-sync` queue nobody drains → mobile txns never synced. Fixed on dev, NOT deployed. Durable gotcha: non-`default` queues silently never run. READ before touching mobile sync / queues.
- [Colleague 4-branch integration (Jun 2026)](colleague-branch-integration-2026-06.md) — 4 feature branches merged into main; prod hit chk_activity_logs_entity_type (fixed da12194, superset migration). CONFIRM prod deploy finished. READ before deploying activity_logs constraint migrations.
- [Attachment file-size fix](attachment-file-size-fix.md) — sizes fixed via S3 HEAD backfill (f6c5cbc). ⚠️ temp /testfilesize + /testfilecheck routes LIVE on prod — remove. Prod owes build + mobile:project-attachments + queue:restart.
- [Clients redesign plan](clients-redesign-plan.md) — agreed IA: tabbed Client Workspace, vendors as per-client tab, 1 "Add client" button. Not started. READ before building clients.
- [Wait for user test before deploy](wait-for-user-test-before-deploy.md) — ⚠️ build, then STOP and wait for Amin's test before checkpoint/deploy. Don't auto-push.
- [Global AI Assistant agent](global-ai-assistant-agent.md) — corner widget → confirm-gated agent on Gemini; Phase 1 done in-repo; includes Windows PHP→Gemini stall gotcha. READ before touching AiAssistantService / AiAssistantChat.
- [Record Keeping AI Voice-to-Edit](record-keeping-ai-voice-edit.md) — voice→AI field-diff→human-approve; 7 locked decisions, 4-phase plan, Phase 1 building. READ before touching Record Keeping detail panel / voice.
- [Client workspace architecture](client-workspace-architecture.md) — Phase 1 DONE (client = single-client accountant session on REAL pages, cp-027 pivot); Phase 2 self-serve personal workspace NEXT. READ before touching the client portal.
- [Marketing site suite](marketing-site-suite.md) — two-project suite; SEO/LLM is marketing-only; platform decision PENDING; NEVER merge marketing into Laravel. READ before marketing/SEO/hosting work.
- [Local dev run (Windows)](local-dev-run-windows.md) — `composer dev` breaks 3 ways on Windows; use the worked launch command. READ before "run localhost".
- [Google OAuth setup](google-oauth-setup.md) — Drive import + Google sign-in share one OAuth client; 3 GOOGLE_* env only; localhost≠127.0.0.1 gotcha. READ before touching Google auth/Drive.
- [Document AI pipeline](document-ai-pipeline.md) — dashboard's own Gemini bulk-extraction (Batch Upload toggle → pending source=ai drafts), Laravel-native, separate from mobile n8n. Phase 1 verified e2e. READ before touching Document Hub upload / Gemini services.
- [Transaction source origin rules](transaction-source-origin-rules.md) — spec NOT built: Source badge by ORIGIN (mobile / client-workspace / accountant) via transactions.mobile_id.
- [Auto-commit leaks secrets](autocommit-leaks-secrets.md) — ⚠️ auto-commit pushes the working tree WITHOUT review (has leaked creds). NEVER write secrets anywhere in the repo.
- [Checkpoint rule](checkpoint-rule.md) — "add a checkpoint" = commit checkpoint-NNN + annotated tag + push branch & tag. Full checkpoint log (019→054) lives in the topic file, not here. Latest noted: cp-054 (ADR-0008 dashboard half, DEPLOYED @ bec0f92).
- [Dashboard build status](dashboard-build-status.md) — feature arc cp003-012 (inbox/invoicing/chat/Messages Studio), gotchas (prod M2M key, MOBILE_FAKE_WRITES, deploy/lockfile). READ AT SESSION START.
- [Mobile attachment binaries missing](mobile-attachment-binaries-missing.md) — mobile rows mirror but S3 objects 404; mobile-side bug (DQ-0014). Use `mobile:attachment-doctor` first.
- [Feature test handoff](feature-test-handoff.md) — every feature: manual test guide + fake-data injection points for Amin (mobile + dashboard).
- [Document each change](document-each-change.md) — every important change gets a docs/ markdown with the same checkpoint.
- [Deploy process](deploy-process.md) — webhook git-pulls + npm-builds (2026-07-13); migrations + Laravel cache clears stay manual. ⚠️ new routes 405 on prod until `optimize:clear` (405-vs-419 curl probe tells stale route cache from a real bug); remember `queue:restart`.
- [Union-plan coordination](union-plan-coordination.md) — "Dashboard Claude" ↔ Mobile Claude via union-plan-coord; ADRs 0001-0004; one-way mobile→dashboard poll-sync.
- [International direction](international-direction.md) — i18n must be RTL-safe + ICU + TMS-friendly from day one.
- [Upload ghost-row bug](upload-ghost-row-bug.md) — read-only s3 disk + throw=>false made silent put() failures → attachment rows with no bytes; fixed via guarded storeUpload(). READ before touching upload/storage code.
- [Handoff: mobile attachment visibility](handoff-mobile-attachment-visibility.md) — 3 fixes made mobile receipts viewable (flysystem dep, key resolver basename fallback, morph-alias normalize); detail in repo docs/. Temp route /temp/s3-attachment-test owed removal. VERIFY state before re-running old prod steps.
- [Client workspace: firm tools + Inbox overhaul](client-workspace-firm-tools-and-inbox.md) — portal Inbox (attachments+voice) + Books/Report/Accounting/Integration exposed per-client (role.min.client + ResolveTenant firewall). On origin/dev, NOT deployed. READ before touching the client portal / exposing a firm tool to clients.
- [Deployment guide](deployment-guide.md) — authoritative doc at docs/DEPLOYMENT.md; Supervisor=queue-only, cron=schedule:run; Docker→Railway plan. Keep updated. READ before deploy/Docker/Railway work.
- [S3 write + Railway migration](s3-write-railway-migration.md) — ⚠️ IN-PROGRESS: prod S3 key read-only → uploads fall to local disk (LOST on Railway redeploy); IAM PutObject fix + temp /temp/s3-write-test route to REMOVE. READ before Railway migration / uploads / storage.
- [CLAUDE.md must stay tracked + .gitignore gotcha](claude-md-gitignore-tracking.md) — dead claude.md ignore rule removed; .gitignore never affects tracked files; don't re-ignore CLAUDE.md.
- [pr staging box](pr-staging-box.md) — pr.voiceaccountant.com = dev-branch staging on prod server; own copy DB va_dashboard_pr (safe to migrate); pg DB creation needs postgres superuser.
- [Bigcapital deployment](bigcapital-deployment.md) — live on Railway; JWT_SECRET weak (fix); migration path is build/ not dist/.
- [Payroll & HR architecture master plan](payroll-hr-architecture.md) — NOT BUILT. Bigcapital + Frappe HR + custom payroll engine (CRA/IRS). READ before any payroll work.
