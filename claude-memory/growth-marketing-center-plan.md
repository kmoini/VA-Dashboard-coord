---
name: growth-marketing-center-plan
description: "AGREED 2026-08-05, not started - Growth & Marketing Center for accountants; Laravel portal = UI/consent/import, company-owned sync2all = sending engine via X-API-Key; own-contacts-only policy. READ before marketing/campaign/referral/contact-import work."
metadata: 
  node_type: memory
  type: project
  originSessionId: f15acdf7-3df5-4965-883a-7b62cd86273c
  modified: 2026-08-15T04:11:57.623Z
---

Plan agreed with Amin 2026-08-05 (from colleague's 6-point "accountants as marketing partners" doc). Build order 1→2→3→4 mandatory; phases 5-6 flexible.

**NUMBERS INTERNATIONAL FINAL 2026-08-15 (commits bc7297c→0e586c2, 65/65 tests, deployed)**: cost-plus pricing GLOBAL (per-number: max(floor, multiplier×telnyx cost) rounded up to $, quoted server-side 1h, checkout quote-only — client never sends price); platform-admin runtime settings at /growth/admin/settings (email caps ×3 + number floors ×2 + multiplier; PlatformSetting KV w/ forever-cache + config fallback; Growth Settings button added to operator toolbar in PlatformSwitcher.jsx); engine item-23 final shape integrated: empty inventory = 200 + emptyReasonCode (10031 filter-empty→[], 10015→no-coverage msg), per-country buyable types via /numbers/countries/:code (GB/SE mobile-only, DE unbuyable) — portal /growth/number/country-types (cached 6h) gates the type buttons, mobile/national types supported end-to-end. ⚠️ remind Amin: optimize:clear after EVERY deploy with new routes (bit him twice).

**DEDICATED NUMBERS SELF-SERVE 2026-08-14 (commits 571d09e→b158a9a, 60/60 tests, deployed)**: /growth/number — search live engine inventory (country selector = SMS-capable countries ONLY per Amin's call, default CA; area-code + toll-free NANP-only), pick, pay monthly Stripe subscription (inline price_data; flat config prices local $5/tollfree $49 env-tunable; engine dropped its church pricing field 6af3d43, costInformation raw passthrough). Provisioning is payment-gated in checkout.session.completed webhook (StripeWebhookController: two DECLARED additive purpose===marketing_number branches); engine-purchase-failure after payment auto-cancels the Stripe sub (never billed for nothing); cancel via Billing portal releases engine number. Active number = firm's SMS fromNumber (fallback shared env number). Engine items 20+21+22 all shipped their side (main 8071239, filters fixed — was flat SDK params so NO filter ever applied; 502 on upstream failure; smsCapable per country). ⏳ OPEN: cost-plus pricing formula for international (flat $5 may lose money in some countries — proposed max($5, 2×telnyx cost) — Amin hasn't decided); numbers purchase E2E test on prod pending.

**LIVE-TEST FIXES 2026-08-14 (commits 1301265, 392c98f, 53/53 tests, deployed)**: (1) handset START now RE-GRANTS revoked sms+voice consent (policy change on Amin's call — self-initiated engine-confirmed START = express consent, source sms_start_keyword; never creates consent for no-history phones, never touches email); (2) engine's two all-blocked 403s are outcomes not failures: quiet-hours → recipients stay queued + fresh delayed blast job at next 09:05 window (2h fallback), all-DNC → suppressed + finalize; voice blast same via buckets (all-refused never marks sent). ⚠️ ENGINE BUG reported to colleague: church keyword handler processes inbound STOP/START on NON-church accounts (church-branded replies on our number; suppression-status stayed clean though). 

**DELIVERABILITY ROUND DEPLOYED 2026-08-13 (commit 366d652, 51/51 tests)** — email is FREE by decision (brand advertising; caps are protection not monetization): A) platform-mailer Reply-To=firm + footer "Powered by VoiceAccountant" links to site; B) EmailThrottle daily caps (connected 300/firm, platform 1000/firm, global 5000 — env MARKETING_EMAIL_*) with release-to-tomorrow + drip spacing (45s/3s) at dispatch; C) {{invite_link}} var (FirmInviteLink service, shared w/ flyer) + composer insert button; D) app-download toggle (email: two HTML buttons from config/marketing.php app_links — real store URLs recorded; SMS: one line → voiceaccountant.com/for-clients.html#app); E) opt-out regexes = sync2all's 3 shared patterns VERBATIM (both layers aligned); F) send-from radio (connected recommended / platform) in meta.email.send_from, honored by job. Branded email layout landed earlier same day: accent header (name always + logo alt-empty), card body, hero image, per-firm accent_color (marketing_identities migration), identity fallbacks (logo→sender avatar_url, phone→firm.phone), growth:preview-email command. ⚠️ prod trap ledger: cron IS the worker; MARKETING_PUBLIC_DISK=s3 still owed on prod.

**✅ FULL E2E VERIFIED IN PRODUCTION 2026-08-13 (checkpoint-237)**: real handset — client sync → mirror → SMS "Hi Amin" + firm ID + signoff + real STOP line → STOP → signed webhook → consent revoked → NO CONSENT in composer. Live-test fixes shipped same day: Add-my-clients sync (clients aren't auto contact-book members), send-bulk REAL contract (userId+fromNumber+recipients+text — verified from engine source), TemplateRenderer keepUnknown mode + translate-before-render (was eating {{first_name}}), STOP check needs real opt-out phrasing not substring "stop" (compliance bug — engine's disclosure validator was ALSO fooled, flagged to colleague), Resend rejects @example.com test recipients. ⚠️ OPERATIONAL: prod has NO supervisor worker for this app — the cron line `queue:work --queue=mobile-sync,default --stop-when-empty --max-time=55` every minute IS the worker (fresh process each run = always new code; queue:restart pointless; supervisorctl needs root). n8n deploy webhook sometimes times out client-side at 10min — server usually completed anyway; verify via second fire or `git log` on server. REMAINING: live email test w/ real address + connected Gmail/MS, voice (engine branch merge + sendVoice grant), Resend events endpoint registration, engine item 20 (numbers UI).

**🚀 DEPLOYED TO PRODUCTION 2026-08-13 (checkpoint-236, commit c450ee8) ON AMIN'S EXPLICIT ORDER** — he overrode the Books freeze as the pact's arbiter (Books session notified via him; its last word had been "freeze holds"). Prod pull+build succeeded (xlsx bundle built). Staging pr also at 784ec82 but its npm install/build steps were superseded by prod. ⏳ POST-DEPLOY MANUAL (Amin, on prod): php artisan migrate (9 Growth migrations, additive) + storage:link + optimize:clear (routes 404 without it!) + queue:restart + env: MARKETING_PUBLIC_DISK=s3 (verify S3 write!), GEMINI ok, SYNC2ALL_* 4 values. THEN the go-live chain: verify POST /webhooks/sync2all/events returns 401 → ping sync2all Claude → it registers webhook same-session → secret straight into SYNC2ALL_WEBHOOK_SECRET + config:clear (short window!) → live E2E (email signature, campaign to own phone, STOP mirror). Voice needs engine branch merge + sendVoice grant. Later: Resend events endpoint registration (RESEND_EVENTS_WEBHOOK_SECRET).

**STATUS 2026-08-05: Phases 1+2 BUILT, TESTED by Amin, checkpoint-206** (commits deb2fde, 45d1b67, 278fc13; docs/growth-marketing-center.md). Shipped: marketing_consents (CASL expiry, revoke-never-silently-regrants, unsubscribe tokens), campaigns/campaign_recipients/content_templates schema, public /unsubscribe/{token} (GET confirms POST revokes), /webhooks/sync2all/optout (HMAC; opt_out revokes sms+voice cross-firm, opt_in only logged), queued SendSmsJob (default queue!) replacing sync invite loop, /growth Overview + Content Library (12 platform templates, firm CRUD), marketing:weekly-tip Mondays 09:00, FEATURES_GROWTH_CENTER kill switch, 10 passing tests in tests/Feature/Growth/. NOT deployed (Books freeze). **Phase 3 DONE, TESTED (101-contact CSV + dedupe), checkpoint-207**: marketing_contacts + marketing_import_batches (attestation verbatim), wizard CSV/xlsx (SheetJS dep)/vCard, dedupe + E.164 + client linking + consent grants (basis attestation), cap 2000/batch warn >1000, delete keeps consent rows. ⚠️ AI PDF path fully REMOVED on Amin's call (commit 4f2ed74 — no Gemini dependency; do NOT rebuild it without asking); orphan ai_prompts row + stale migrations row on Amin's local DB are harmless. vCard path untested by Amin yet. **Phase 4 EMAIL DONE, TESTED locally by Amin (5-recipient send + schedule/draft/cancel), checkpoint-208** — ⏳ connected-email (Gmail/MS) send path still to be verified ON LIVE after deploy: /growth/campaigns composer (template picker, variables, consent-gated audience picker), CampaignDispatchService (suppressed rows w/ reasons) + SendCampaignEmailJob (consent re-check at send; connected Gmail/MS account, Mail fallback), CASL footer + unsubscribe per recipient, open pixel /t/o/{token} + HMAC-signed click /t/c/{token}, campaigns:dispatch-due cron 5min, per-recipient report, /webhooks/resend/events (SEPARATE Svix secret RESEND_EVENTS_WEBHOOK_SECRET per Books pact; complaint revokes consent). Growth suite 24/24. **sync2all work package: ALL 17 items DONE 2026-08-05** (branch feature/accountant-vertical-readiness, 478 tests, NOT deployed; details in its MIGRATION-NOTES.md). Integration contract: (1) contacts/import is BREAKING — requires top-level attestation {attestedBy, attestedAt, text} + per-row optInStatus/consentSource/consentTimestamp; rows without consent → status pending_consent, not messageable. (2) API keys SHA-256 only — old keys dead; portal key needs manageContacts+manageCampaigns+viewCompliance perms (Amin generates post-deploy via POST /api/sms/api-keys, env only). (3) New: POST /api/sms/campaigns/:id/execute (server-side + real scheduling) + POST /api/compliance/suppression-status {phones[]} max 1000. (4) Webhook events contact.opted_out/opted_in {phone, channel, status, source, timestamp} via their generic webhook infra — our /webhooks/sync2all/optout receiver must be ADAPTED to their envelope/signing (spec requested). (5) Personalization tokens resolve from sync2all sms_contacts → plan: mirror portal contacts into sync2all at import time. (6) ⚠️ VOICE NOT REACHABLE for accountant vertical (church-only routes) — decided v1 ships without voice; follow-up item 18 requested: generic /api/voice/* with explicit phone array + shared voice-recipients.js gate. (7) Agreed suppression strategy: webhook mirror for UI + suppression-status call right before each blast. **FEEDBACK ROUND 1 FIXED 2026-08-06 (commit 1bef4b6, 40/40 tests)** from Amin's full test: share composer (editable caption + template picker + X/LinkedIn/FB/WhatsApp/Telegram/Instagram, copy-post-then-open pattern), import Address+Additional-info mappable (meta), auto-create firm Clients on import (source=growth_import, opt-out checkbox, matches ClientsController@store shape), Select-all-shown checkbox in composer audience, connect-email banner (email channel, links Settings>Integrations), flyer link overflow fixed, referral Accept page restyled (brand band + benefits), template Copy=body-only + new Duplicate action. **Items 11+12+13 BUILT 2026-08-06 (commit bcb0338, 44/44 Growth + 22/22 AI tests), AWAITING Amin's test**: /growth/identity (logo on marketing_public_disk env MARKETING_PUBLIC_DISK default 'public' — prod MUST set s3 + verify S3 write; phone; custom email signature w/ vars; sms_signoff ≤60) wired into email renderer (logo+signature BEFORE non-removable CASL footer), SMS blast (signoff before firm-name/STOP guarantees), flyer (logo+phone); email header image (5MB, hero above body, meta.email.image_url); AI post studio: marketing-post.generate prompt (catalog appended at END after Books' entries; first_name declared as literal-passthrough optional placeholder for the sync test; publish migration ran), MarketingPostGenerator (CallsAiForJson, 3 variants, feature post_studio/step post.generate), POST /growth/generate-post throttle 10/min, Generate buttons on share composer (social) + campaign composer (email fills subject+body, sms body). Local dev needs `php artisan storage:link` once for logo/image URLs. SMS MMS deferred until engine bulk-MMS contract. ⏳ AMIN ENV ACTIONS: Google OAuth redirect_uri_mismatch = add local redirect URI to console (localhost vs 127.0.0.1 gotcha per [[google-oauth-setup]]); MS OAuth env creds unset; SYNC2ALL_* 4 values he holds can be set in LOCAL .env NOW for real SMS testing (engine is live). ⏳ COLLEAGUE ITEM 19 proposed: generic /api/numbers/* (search/purchase/list/release per account) so accountants buy numbers from the portal. Amin: "بعد از اینها بقیه موارد باقی‌مونده /growth رو انجام می‌دیم".

**Phase 5 VOICE BUILT 2026-08-06 (commit cc48ea5), AWAITING Amin's test**: composer Voicemail channel (ringless/broadcast toggle, MediaRecorder in-browser recording or upload mp3/wav/m4a/webm 15MB, voicemail-script templates as reference), import now grants VOICE consent alongside SMS on phone attestation, dispatch gates on voice consent → ONE SendCampaignVoiceBlastJob (lazy asset upload cached in meta.voice.asset_id, pre-blast suppression check, single ringless/broadcast call, 4 engine blocked* buckets in meta.sync2all_voice shown on report). Sync2allClient +uploadVoiceAudio (explicit multipart — attach() broke under Http::fake) +sendVoice (parses 403 all-refused shape). 39/39 Growth tests. ⚠️ Amin DEFERRED ALL testing of phases 5+6 to a single comprehensive Persian test guide at the end; NO checkpoints for phases 5/6 until he tests. Voice E2E additionally needs: engine branch merge + sendVoice grant via PUT api-keys.

**Phase 6 BUILT 2026-08-06 (commit 61d07a4), AWAITING Amin's test** (no checkpoint yet): referral_rewards ledger (1 month credit per converted referral, idempotent detect via unique referral_sign_up_id, daily referrals:detect-rewards 07:30 + inline sweep; pending→admin approve/deny→manual Stripe credit→mark applied — ledger NEVER touches billing), GrowthScoreService (invites×2/accepts×10/contacts×1/campaigns×15/signups×25/converts×100; Starter/Advocate/Champion/Ambassador; leaderboard uses bulk grouped queries), /growth/rewards + /growth/admin/leaderboard (platform.admin) + score strip on overview, /growth/flyer co-branded dompdf one-pager with reused 2-year link-type ReferralToken, 3 case-study templates seeded (fill-in-the-blanks, no fake data). NotificationService TYPE_REFERRAL_REWARD added. 34/34 Growth tests.

**sync2all E2E VERIFIED IN PRODUCTION 2026-08-06 (real SMS: import→suppression→execute→{{firstName}} rendered→STOP→403 on re-send→key least-privilege)**. Amin holds 4 env values: SYNC2ALL_BASE_URL / API_KEY / ACCOUNT_UID / FROM_NUMBER (all plumbed in config/services.php). ⚠️ Two-layer opt-out: Telnyx carrier-level STOP block is INDEPENDENT of suppression-status (their store only); un-suppress needs handset START. **Item 18 BUILT on sync2all branch feature/generic-voice-routes (2026-08-06), NOT deployed — ships on our request when Phase 5 starts.** INDEPENDENTLY VERIFIED 2026-08-06 in the local repo: branch HEAD b0562c97 (1 commit, +1211 lines incl. MIGRATION-NOTES-VOICE.md + unit/integration tests), origin/main still 3bf9278 (item 18 not in prod), strict-consent line confirmed (`contact.optInStatus !== true` — blocks string "true"). STRICT consent as we specified (boolean optInStatus===true only; string "true"/missing/false all block) then DNC→STOP→quiet-hours. Contract in docs/growth-marketing-center.md §Phase 5 prep: /api/voice/broadcast + /ringless {recipients[], audioAssetId|audioUrl}, max 5000; /api/voice/audio multipart upload 15MB mp3/wav/m4a/webm → assetId; response has 4 blocked* buckets incl. blockedUnsubscribed. ⚠️ After its deploy: grant sendVoice WITHOUT rotating the key — PUT /api/sms/api-keys/:id {"permissions":{"sendVoice":true}} (merges, preserves other 5 perms). **sync2all is LIVE, portal API key ISSUED (Amin holds it), webhook NOT registered yet** — registration waits until the portal deploys and /webhooks/sync2all/events returns 401 (404 starts their failure counter). Go-live runbook with exact order in docs/growth-marketing-center.md §Phase 4b. ⚠️ webhook signing secret returned ONCE, never recoverable — straight to env; auto-disable after 10 failures is recoverable (PUT {"status":"active"} resets failureCount); registration→env window must be SHORT (inbound STOPs fire events regardless of campaigns). Receiver ACKs fast + queues ProcessSync2allEventJob (commit 1b9e788). Ping sync2all's Claude when freeze lifts; they register same-session.

**Phase 4b DONE, TESTED locally by Amin, checkpoint-209** (post-deploy runbook in docs/growth-marketing-center.md): webhook receiver rewritten to their envelope (route /webhooks/sync2all/events, X-Webhook-Signature HMAC hex over raw body, {event,timestamp,data}, stale-retry guard on data.timestamp); Sync2allClient (importContacts strict-boolean optInStatus / suppressionStatus 1000-chunks / sendBulk ONE request per blast — their limiter is per-IP / registerWebhook, secret to env once); MirrorContactsToSync2allJob after every import (consent provenance verbatim); SMS campaigns in composer (channel toggle, segment counter, per-channel consent audience) → ONE SendCampaignSmsBlastJob: firm-level var render + snake→camelCase token translation ({{first_name}}→{{firstName}}), guaranteed firm name + STOP line, pre-blast suppression check, engine block counts in campaign meta. 29/29 Growth tests. END-TO-END BLOCKED until sync2all branch feature/accountant-vertical-readiness merges+deploys (Amin does both; Firestore indexes FIRST per their MIGRATION-NOTES §1, audit stale sms_campaigns scheduledAt §8), then Amin: generate portal API key (5 perms: sendSMS viewMessages manageContacts manageCampaigns viewCompliance) + register webhook via Sync2allClient::registerWebhook + set SYNC2ALL_BASE_URL/API_KEY/WEBHOOK_SECRET in env. Google/Microsoft Contacts OAuth import REMOVED from the plan 2026-08-05 (Amin + my agreement: Google sensitive-scope verification hassle + risk to the shared OAuth client outweigh a convenience feature; CSV/Excel/vCard suffice — do not propose it again).

**Hard policy (Amin + colleague):** accountants message ONLY their own existing clients/contacts, never cold lists. This puts us in CASL implied-consent / TCPA EBR safe zone. Enforce in product: attestation at import, volume caps/flags (~1000+ contacts = review), controlled import paths.

**Architecture:** va-dashboard2 (Laravel) = source of truth for clients, consent, attestation, Growth Center UI, import wizard, template library, referral, weekly tip, reports. sync2all (E:\Projects\sync2all — OWNED BY OUR COMPANY, no business agreement needed) = sending engine: SMS/RVM/voice/bulk-email, DNC/litigator, quiet hours, STOP/START, email suppressions, link tracking. Laravel calls it with X-API-Key (works on all authenticateUser routes), resolves recipients itself and passes arrays to send-bulk — no permanent contact sync between DBs. Opt-in authority = Laravel; opt-out/DNC authority = sync2all (owns Telnyx webhook); sync2all fires opt-out webhook back → Laravel mirrors in UI. Later: migrate dashboard's existing invite-SMS Telnyx path onto sync2all (one Telnyx account, one 10DLC, one opt-out store).

**Key audit findings (2026-08-05):**
- va-dashboard2 already has ~60%: BulkInviteController (50 email/200 SMS caps, sync foreach — needs queueing), ReferralToken + AccountantReferralCode (+/referrals page not in sidebar — seed of Growth Center), ConnectedEmailService (send as accountant's own Gmail/Microsoft — deliverability gold), TelnyxSmsService, notification+scheduler+feature flags. MISSING: any consent/unsubscribe model (zero fields — biggest gap), per-recipient send tracking, Campaign/Template models, xlsx.
- sync2all defects → colleague's Claude got a full P0/P1/P2 work-package prompt (consent hardcoded true on import, send-bulk skips optInStatus, voice/RVM ignore smsConsent, church email ignores suppressions, referral track endpoint forgeable + credit endpoint no ownership check, scheduledAt never executed, {{tokens}} never rendered, audio = URL-only). Colleague does sync2all side; item 14 = opt-out webhook + batched suppression-status endpoint is OUR integration point (get URL/payload + an API key for the portal — env only, repo auto-pushes).
- Mobile app = clients only, no accountant login, no contacts permission → "phone contacts" import is web-based: Google People API / MS Graph contacts (extend existing EmailConnection OAuth), vCard .vcf upload, plus CSV (extend BulkSmsInviteModal parser into shared wizard: mapping, E.164, dedupe vs clients+tokens), xlsx via SheetJS client-side, PDF via DocumentAiExtractor::ocrToText + new AiPromptCatalog entry + new AiFeature case + review step (do NOT reuse DocumentAiIngestService — it writes transactions).

**Phases:** 0: legal ToS/AUP + 10DLC (Amin, non-dev). 1 (Laravel): consent per-channel (source/timestamp/CASL 2yr implied-consent expiry), unsubscribe token+public page, Campaign/CampaignRecipient/ContentTemplate models, queued Telnyx. 1b (colleague, parallel): sync2all work package. 2: Growth Center sidebar section behind feature:growth_center flag, fold /referrals in, template library, share buttons, weekly tip (routes/console.php; carve-out from NotificationService 24h dedup). 3: import wizard (CSV/xlsx/vCard/PDF-AI/Google contacts) + attestation. 4: email campaigns (connected account, open/click, bounce via NEW Resend endpoint with RESEND_EVENTS_WEBHOOK_SECRET — see [[growth-books-session-pact]]) + SMS campaigns via sync2all. 5: RVM/voice via sync2all; voice cloning (ElevenLabs, nowhere yet) later with disclosure. 6: advocate program (subscription-credit rewards, co-branded content).

Commit prefix `growth:`. Respect [[wait-for-user-test-before-deploy]] and no-deploy rule while Books overhaul is unfinished. Related: [[growth-books-session-pact]], [[books-accounting-overhaul-plan]], [[client-registry-multi-company]]
