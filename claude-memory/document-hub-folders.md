---
name: document-hub-folders
description: "Document Hub redesign (2026-07-13) — real folders + AI auto-filing + Explorer UI; BUILT on dev, migrated+built locally, NOT deployed, awaiting Amin's test. Read before touching /documents, DocumentFolder, attachment folder_id, or doc classification."
metadata: 
  node_type: memory
  type: project
  originSessionId: 31b05655-8274-4aa9-937b-318a8d62e752
---

Document Hub redesign built 2026-07-13, all smoke checks green. **DEPLOYED as checkpoint-139 (commit b592961, Amin-approved)** — prod webhook pulled AND ran vite build. Post-deploy incident: bulk move failed on prod ("Could not move the file(s)") → diagnosed via unauthenticated curl probes as **stale route cache** (new POST routes answered 405, old upload route 419); fix = `php artisan optimize:clear` + `migrate --force` + `queue:restart` on the server (full story in [[deploy-process]] and the repo doc). Docs commit 9f444d5 records the deployment history. ⚠️ checkpoint-139 was a PARTIAL stage: the uncommitted Resend dual-driver email work (ResendInboundWebhookController, ResendInboundClient, InboundEmailService, SettingsController, bootstrap/app.php, EmailForwardingCard + email hunks of routes/web.php & routes/console.php) was deliberately left OUT via hunk-level staging (`git apply --cached`) and still sits uncommitted in the working tree — it needs its own checkpoint. Full doc + manual test guide: `va-dashboard2/docs/document-hub-folders.md`.

Shape:
- `document_folders` table: 10 SYSTEM folders (firm_id NULL, slug, seeded **inside the migration**, no seeder step) + user folders (firm_id set; client_id set = client-workspace folder). **Unsorted is virtual = attachments.folder_id NULL** (old files intentionally un-backfilled → all start Unsorted; that was Amin's explicit choice).
- attachments += folder_id / doc_category / doc_category_confidence / **folder_assigned_by ('rule'|'ai'|'user')** — a `user` filing is STICKY, AI/rules never overwrite it (`DocumentClassifier::apply`).
- Filing chain: AttachmentObserver (audio→voice_notes, every write path, zero cost) → extraction call now returns top-level `doc_category(+confidence)` (schema REQUIRED fields; applied in DocumentAiIngestService incl. per-page split, checksum-reuse replay, and PollDocumentAiBatchesJob via `parseBatchDocCategory`) → `ClassifyDocumentJob`/`DocumentClassifier` flash-lite call for non-extract uploads (Document Hub, client portal upload, scanner) → filename-keyword fallback → **below `DOC_CLASSIFY_MIN_CONFIDENCE` (0.6) stays Unsorted** (accuracy-first rule Amin cares about most).
- Client workspace rules (Amin-decided): client sees system+firm+own folders, creates own (client_id-scoped), **moves/deletes ONLY files their side created** — `Attachment::isClientMovable()` (uploader is portal user of same client OR mobile sync; email/accountant files locked). destroy() hardened for portal too.
- UI: folder rail + Unsorted badge + 4 view densities (localStorage `dochub.view`) + debounced server search + multi-select/drag-drop move + real pagination; **MOCK_DOCUMENTS and the fake Web-Audio tone were removed**. Documents stays OUT of the sidebar (Amin: keep the Record-Keeping "Browse all documents" link only).
- New activity_logs constraint migration adds `DocumentFolder` entity (full-superset redefinition — see [[activity-log-entity-type-trap]]).

Same-day additions (Amin's asks): **folder-rail filter box** (client-side "Filter folders…") and **Trash** — soft delete is now a full flow: bulk delete (`POST /documents/bulk-delete`) + drag-onto-Trash + confirm modal; Trash rail entry (`?folder=trash`, onlyTrashed scope) with per-file/bulk **Restore** (`POST /documents/restore`, audit action `reverted`); restore keeps prior folder; portal ownership rule applies to delete AND restore; `documents.serve` is `withTrashed()` so Trash thumbnails render. **30-day retention**: nightly `documents:purge-trash` @03:15 (env `DOC_TRASH_RETENTION_DAYS`, 0=off) deletes BYTES + stamps `permanently_deleted_at` (metadata row KEPT — the base-schema column, finally used; Trash scope + restore exclude purged rows). Purge guards: never deletes from the mobile bucket; skips objects another live row references; saveQuietly (no observer). Purge writes NO activity_logs row on purpose — user_id-null system writes trip chk_activity_logs_user_or_ai unless action_source='ai_worker'; the stamped column + Log::info are the record.

Prod deploy steps owed: `migrate --force` + `npm run build` + `config:clear` + `queue:restart`. New env knobs (optional): DOC_AUTO_CLASSIFY, DOC_CLASSIFY_MIN_CONFIDENCE.

2026-07-13 second wave — **DEPLOYED as checkpoint-140 (5923099)**, prod webhook pulled + built; Amin owed `optimize:clear` on the server for the new move-undo route: (1) "New" pill on files uploaded since last visit, clears on click — pure localStorage (`dochub.lastVisit`/`visitBaseline`/`seenNew`), per-browser by Amin's choice; server adds `uploaded_at_ts`. (2) Upload-date range filter — `date_from/date_to` on GET /documents (inclusive created_at days) + toolbar date inputs; doc's OWN date (`doc_date`) deliberately deferred (needs extraction schema + backfill). (3) Move Undo — move() returns `previous` per-file {folder_id, assigned_by} (+ logged in audit changes); 10s toast bottom-right; new `POST /documents/move-undo` restores folder_id AND folder_assigned_by (mis-moved AI file goes back to 'ai', not sticky-'user'); audit action `reverted`, no new CHECK values. No migration; deploy needs optimize:clear (route cache) + npm build. Doc: `docs/document-hub-new-badge-date-filter-undo.md`. ⚠️ working tree also still holds the uncommitted Resend email work — keep them in SEPARATE checkpoints.

2026-07-13 third wave: **NESTED FOLDERS (max 3 levels) — DEPLOYED as checkpoint-141 (482c948)**, webhook pulled + built. ⚠️ Prod still owes (Amin, SSH): `migrate --force` (parent_id column — subfolder creation FAILS until then; page itself loads), `optimize:clear` (covers cp-140's move-undo route AND the Resend webhook route), `queue:restart`. NOTE: the uncommitted Resend email work got swept into commit **e07478d "Document Redesign"** by the auto-commit (misleading message, content = email dual-driver) and rode along to prod with this deploy — check EMAIL_INBOUND_DRIVER env before relying on it. Amin's 5 locked decisions: depth cap 3; subfolders inside system folders allowed; rail badge = subtree `count_total` but folder view lists direct files only (+ subfolder strip); delete-with-children BLOCKED (422, no cascade, no promote); AI keeps targeting only the 10 top-level system slugs. Shape: `document_folders.parent_id` (migration 2026_07_13_000003), `DocumentFolder::MAX_DEPTH/parent/children/depth()`, storeFolder takes `parent_id` (visible-in-scope + depth<3; duplicate-name check now PER-SIBLING-GROUP, was scope-global), index() adds parent_id+count_total (rollup over visible set only). UI: tree rail (localStorage `dochub.expanded`, auto-expand ancestors), breadcrumb path, subfolder strip w/ drag-drop, "New subfolder" toolbar button (hidden at depth 3), tree Move-to list, blocked-delete modal, depth-first mobile chips. Deploy owes `migrate --force` + build + optimize:clear. Doc: `docs/document-hub-nested-folders.md`. Working tree ALSO still holds uncommitted Resend email work — separate checkpoints.

Related: [[document-ai-pipeline]], [[batch-upload-ai-tester-fixes]], [[ai-usage-monitoring]] (new step `document.classify`), [[gemini-model-policy]] (classifier reads services.gemini.model_file).
